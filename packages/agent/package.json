{
  "name": "@mcp-events/agent",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Serverless Strands Agent Lambda — processes MCP events from both servers",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "3.1056.0",
    "@aws-sdk/lib-dynamodb": "3.1056.0",
    "@deliveryhero/dynamodb-lock": "*",
    "@mcp-events/shared": "0.0.1",
    "@strands-agents/sdk": "*",
    "zod": "*"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.147"
  }
}
