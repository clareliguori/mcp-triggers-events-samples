{
  "name": "@mcp-events/subscription-manager",
  "version": "0.0.1",
  "private": true,
  "description": "Subscription Manager Lambda — creates and refreshes per-customer webhook subscriptions",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@aws/run-mcp-servers-with-aws-lambda": "*",
    "@mcp-events/shared": "0.0.1",
    "@modelcontextprotocol/sdk": "*",
    "zod": "*"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.147"
  }
}
