{
  "name": "@mcp-events/mcp-server-core",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Shared MCP Events server core — clients, env, subscription store, webhook delivery, JSON-RPC transport, and dual-trigger dispatch reused by the USGS and Scheduler MCP servers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "3.1056.0",
    "@aws-sdk/client-kms": "3.1056.0",
    "@aws-sdk/lib-dynamodb": "3.1056.0",
    "@mcp-events/shared": "0.0.1",
    "standardwebhooks": "*",
    "zod": "*"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.147",
    "aws-sdk-client-mock": "4.1.0"
  }
}
