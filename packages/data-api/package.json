{
  "name": "@mcp-events/data-api",
  "version": "0.0.1",
  "private": true,
  "description": "Shared Data API Lambda — customer config, subscriptions, and reports persistence",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@mcp-events/shared": "0.0.1",
    "zod": "*"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.147"
  }
}
